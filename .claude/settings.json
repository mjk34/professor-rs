{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); echo \"$INPUT\" | grep -q 'git commit' && grep -rq '// \\[TEST\\]' src/ 2>/dev/null && echo 'WARNING: [TEST] guards are still active — re-enable market hours/cooldown checks before committing' || true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cargo check 2>&1 | tail -20"
          }
        ]
      }
    ]
  }
}
